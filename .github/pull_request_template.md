<!-- The PR title becomes the squash-commit subject on main — write it as an
     imperative sentence saying what changed. The body becomes the commit
     message. -->

## What

## Why

## Checks

- [ ] `npm run lint` and `npm run build` pass locally (CI runs both on the PR)
- [ ] New migrations (if any) applied to dev and committed
- [ ] CLAUDE.md phase checklist / current state updated if a step completed
